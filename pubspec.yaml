name: flutter_application_arda
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.5.3

dependencies:
  flutter:
    sdk: flutter
  flutter_spinner_time_picker: ^0.2.1
  time_picker_spinner: ^1.0.0
  http: ^0.13.4

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0
  hexcolor: ^2.0.7

flutter:
  
  uses-material-design: true
  assets:
    - assets/images/
    - assets/icons/
